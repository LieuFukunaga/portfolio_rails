%h1 トップページ
- if user_signed_in?
  = "#{current_user.name}"
  %br
  = "#{current_user.email}"
  %span
  %br
  = link_to destroy_user_session_path, method: :delete do
    %i.fas.fa-sign-out-alt
    %span ログアウト
- else
  %h2 ログインしていません
  =link_to "新規登録", new_user_registration_path
  =link_to new_user_session_path do
    %i.fas.fa-sign-in-alt
    %span ログイン

-# %p
-#   = $date
-# %p.non-modal
-#   = button_tag "モーダルとは無関係なボタン", type: "button", id: "out-of-modal"

-# %p.modal
-#   = button_tag "modal", type: "button", id: "modal-btn"
-# #modal-box
-#   = button_tag "close", tyoe: "button", id: "modal-close-btn"

%p
  = link_to new_list_path, class: "new-list-btn" do
    %i.fas.fa-plus-circle
    %span リストを追加

.lists
  = "#{current_user.name}さんのリスト一覧"
  - if current_user.lists.length != 0
    - current_user.lists.each do |list|
      %p.list
        = link_to list_path(list) do
          %i.fas.fa-angle-double-right
          = "#{list.list_name}"
  - else
    %p -- NO LIST --